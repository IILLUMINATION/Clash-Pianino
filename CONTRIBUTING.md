# Правила внесения изменений в проект
При любом коммите и пуше необходимо соблюдать правила описанные в этом файле

## Структура
```
Clash-Pianino/
    client/
        Cargo/rust проект, Cargo.lock и target в gitignore
    server/
        Бэкенд а на чём бэкенд кста?
    shared/
```


Для работы нужно использовать ветки, в ветке main находится только стабильный код.

```
feat/client/core-loop — базовая механика (высадка юнитов).

feat(client/core-loop): Лялялял
ж

feat/server/matchmaking — логика поиска игроков.

feat/shared/proto-v1 — описание протокола связи.
```

    что думаешь?

    что думаешь?

## Правила оформления коммитов
Каждый коммит в репозитории должен содержать префикс, формат коммита:

```
префикс(где именно): какие-то изменения
```

### Префиксы коммитов
- **feat**: Новая функциональность. При feat коммите необходимо сменить semver в **client/Cargo.toml** (Либо Cargo.toml другого крейта в который были внесены изменения. Если часть проекта куда были внесены изменения не использует cargo, то версия там не меняется). Изменяется именно **минорная** версия, то есть 0.X.0 где x это минорная версия. Пример: 0.2.0 -> 0.3.0
- **fix**: Это исправление бага (patch). При таком коммите необходимо изменить патч версию в semver Cargo.toml. То есть 0.0.X где X это патч версия. Пример: 0.2.0 -> 0.2.1
- **docs**: Изменения в документации. Сюда относятся изменения изменения в документации функций/структур/перечислений в rust/другом коде (то есть `///`), изменения в README.md, STYLE_GUIDE.md, CONTRIBUTING.md либо любой другой текст, который нужен только для прочтения его человеком
- **style**: Форматирование кода. Добавление отступов, переносов строк, пробелов и так далее, всё что связано с стилисткой кода
- **refactor**: Рефакторинг кода без изменения функциональности. Распутывание спагетти-кода, разбиение на более мелкие функции и в целом упрощение кода для улучшения качества кода для улучшения его понимания
- **perf**: Улучшения производительности (Время выполненения, запуска и потребление ОЗУ/Видеопамяти)
- **test**: Добавление или изменение тестов
- **build**: Изменения в системе сборки (Добавление скриптов в папку scripts, изменения в Cargo.toml не связанные с изменением версии крейта)
- **ci**: Настройки CI (Github actions в данном случае, но также это может и означать изменения в gitlab ci и других если они будут)
- **chore**: Рутинные задачи (Обновление версий зависимостей)